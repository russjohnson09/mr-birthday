[project]
name = "python-scripts"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    # uv add midi2audio
    "midi2audio>=0.1.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
